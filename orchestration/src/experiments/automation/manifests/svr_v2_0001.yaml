experiment_id: svr_v2_0001
description: "Turn 2 — tight search around defaults. SVR is very sensitive: narrow C to [0.1, 10.0], epsilon to [0.01, 0.5]. Rely on defaults for kernel=rbf, gamma=scale."
model_name: svr
baseline_experiment: svr_baseline
split_seed: 42
primary_metric: validation_avg_rmse
search_method: random
allowed_hyperparameters:
  - name: C
    type: numeric
    range: [0.1, 10.0]
  - name: epsilon
    type: numeric
    range: [0.01, 0.5]
maximum_trial_count: 30
maximum_wall_clock_minutes: 120
maximum_test_finalists: 3
minimum_improvement_threshold: 0.005
per_trial_timeout_minutes: 10
artifact_output_location: artifacts/experiments
