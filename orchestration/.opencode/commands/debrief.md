---
description: Capture project status
agent: build
---

Our work is done. Generate notes that capture short-term, local information that will allow another person to start where we left off. The other person has access to the larger project context, so we just need to capture critical information from this conversation only, if any. Update the following: ./.opencode/memory.md and ./docs/$ARGUMENTS.  