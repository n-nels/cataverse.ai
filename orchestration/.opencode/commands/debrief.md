---
description: Capture project status
agent: build
---

Our work is done. Generate notes that capture short-term, local information that will allow another agent to start where we left off. The other agent has access to the larger project context, so we just need to capture critical information from this conversation only, if any. Update the following: ./.opencode/memory.md and ./docs/$ARGUMENTS.  